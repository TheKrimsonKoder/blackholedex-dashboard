name: Daily Fetch

on:
  schedule:
    - cron: "0 14 * * *"   # 14:00 UTC daily (adjust if you like)
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fetch-and-commit:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout repo (main, full history)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests pandas

      - name: ▶️ Run your existing fetch
        run: python scripts/api_fetch.py

      - name: 🧾 Build daily summary (auto-competitor)
        run: python scripts/build_summary.py

      - name: 📤 Quick output preview
        run: |
          echo "---- daily_summary.txt ----"
          cat data/daily_summary.txt || true
          echo
          echo "---- black_data.csv (tail) ----"
          tail -n 10 data/black_data.csv || true

      - name: 💾 Commit and push updated data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/*.csv data/*.txt data/*.json 2>/dev/null || true
          git commit -m "Daily update: $(date -u +"%Y-%m-%d %H:%M:%S UTC")" || echo "No changes to commit"
          git push
